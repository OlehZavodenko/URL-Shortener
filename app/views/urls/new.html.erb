<h1>New Url</h1>

  <% @url.errors.each do |attr, msg| %>
  <%= msg %> on <%= attr %>
  <% end %>

  <%= form_for @url do |f|  %>
    <p>
    <%= f.label 'Original:' %>  <%= f.text_field :original %>
    </p>

    <p>
    <%= f.hidden_field :random_id, :value => @url.random_id %>
    <%= f.submit "Shortern my URL" %>
    </p>
  <% end %>
